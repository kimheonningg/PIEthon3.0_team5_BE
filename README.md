## How to run this project

1. Install dependencies by:

```bash
pip install -r requirements.txt
```

2. Launch by:

```bash
python server.py
```

## Tools used (Tech stack)

Postman (API testing)
MongoDB (database)
